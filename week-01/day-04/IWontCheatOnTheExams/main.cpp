#include <iostream>
#include <string>

int main(int argc, char* args[]) {
    std::string text = "I won't cheat on the exam!";
    for (int i = 0; i < 100; i++){
        std::cout << text << std::endl;
    }
    // Create a program that writes this line 100 times:
    // "I won't cheat on the exam!"

    return 0;
}